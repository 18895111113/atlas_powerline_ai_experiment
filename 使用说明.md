# 输电线路智能巡检项目从零运行手册

本文档面向第一次接手项目的人，目标是让对方可以从一台 PC 和一块 Atlas 开发板开始，把项目完整跑起来。

项目最终效果：

- PC 浏览器打开前端页面。
- Atlas 开发板运行 Flask 后端和 OM 模型。
- 前端可以连接开发板摄像头实时检测。
- 前端可以上传图片、上传视频，并显示 Atlas 后端画好的检测结果。

## 1. 项目目录说明

建议先确认项目根目录大致如下：

```text
atlas_powerline_ai_experiment/
  configs/                         类别文件和数据集配置
  datasets/                        训练和测试数据，可选
  docs/                            项目文档
  frontend/                        浏览器前端，纯静态页面
  models/                          PC 侧模型文件
    best.pt                        训练后的 PyTorch 权重
    onnx/best.onnx                 训练后导出的 ONNX
  scripts/                         PC 和 Atlas 辅助脚本
  src/
    atlas/                         Atlas 后端、OM 推理、后处理
    pc/                            PC 训练、导出、数据检查脚本
  yolov5/                          YOLOv5 官方训练/导出代码，可选
  atlas_deploy_powerline_yolov5s/  上板部署目录
```

其中最终演示最关键的是：

```text
frontend/
atlas_deploy_powerline_yolov5s/
atlas_deploy_powerline_yolov5s/src/atlas/api_server.py
atlas_deploy_powerline_yolov5s/models/om/powerline_yolov5s.om
atlas_deploy_powerline_yolov5s/configs/classes.txt
```

## 2. 设备和软件要求

PC 侧建议：

- Windows 10/11
- Python 3.10 或 3.12
- PowerShell
- 如果要重新训练：NVIDIA GPU + CUDA 版 PyTorch

Atlas 侧建议：

- Atlas 200I DK A2 或同类 Ascend 设备
- CANN/Ascend Toolkit 已安装
- Python 3
- 能通过网络被 PC 访问
- 摄像头能被 OpenCV 以 `camera-source 0` 打开

下面示例默认：

```text
Atlas 开发板 IP: 192.168.137.100
Atlas 后端端口: 8000
PC 前端端口: 5173
```

如果你的开发板 IP 不同，请把命令里的 `192.168.137.100` 换成实际 IP。

## 3. PC 侧环境准备

在 PC PowerShell 中进入项目根目录：

```powershell
cd C:\Users\29951\OneDrive\Desktop\atlas_powerline_ai_experiment
```

创建虚拟环境：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

安装 PC 依赖：

```powershell
python -m pip install --upgrade pip
python -m pip install -r requirements/pc.txt
```

如果后面要重新训练 YOLOv5，还需要安装 YOLOv5 依赖：

```powershell
python -m pip install -r .\yolov5\requirements.txt
```

## 4. 快速演示路径：不重新训练

如果项目里已经有：

```text
models/onnx/best.onnx
atlas_deploy_powerline_yolov5s/models/om/powerline_yolov5s.om
```

则可以跳过训练和导出，直接从第 7 节开始上板运行。

这是最适合答辩、演示、交接验收的路径。

## 5. 从数据集重新训练，可选

如果只是运行现有模型，可以跳过本节。

先检查数据集：

```powershell
python .\src\pc\check_dataset.py --dataset-root .\datasets\powerline --num-classes 3
```

确认 `configs/classes.txt` 内容为 3 类：

```text
crane
excavator
foreign_object
```

开始训练。显存较小可以把 `--batch` 调低，例如 4：

```powershell
python .\src\pc\train_yolov5.py `
  --yolov5-dir .\yolov5 `
  --data .\configs\powerline_data_abs.yaml `
  --weights yolov5s.pt `
  --imgsz 640 `
  --batch 4 `
  --epochs 100 `
  --project runs_powerline `
  --name yolov5s_powerline_gpu `
  --device 0
```

训练完成后，通常会得到：

```text
runs_powerline/yolov5s_powerline_gpu/weights/best.pt
runs_powerline/yolov5s_powerline_gpu/weights/last.pt
```

把最好的权重复制到统一模型目录：

```powershell
Copy-Item .\runs_powerline\yolov5s_powerline_gpu\weights\best.pt .\models\best.pt -Force
```

## 6. 导出 ONNX，可选

如果已有 `models/onnx/best.onnx`，可以跳过本节。

从 `models/best.pt` 导出 ONNX：

```powershell
python .\src\pc\export_onnx.py `
  --yolov5-dir .\yolov5 `
  --weights .\models\best.pt `
  --imgsz 640 `
  --opset 12
```

如果导出的 ONNX 出现在 `models/best.onnx`，复制到正式位置：

```powershell
New-Item -ItemType Directory -Force -Path .\models\onnx | Out-Null
Copy-Item .\models\best.onnx .\models\onnx\best.onnx -Force
```

确认 ONNX 存在：

```powershell
Test-Path .\models\onnx\best.onnx
```

预期输出：

```text
True
```

## 7. 准备上板部署包

当前上板目录是：

```text
atlas_deploy_powerline_yolov5s/
```

如果你修改了根目录的 `src/atlas/api_server.py`，需要同步到部署目录：

```powershell
Copy-Item .\src\atlas\api_server.py .\atlas_deploy_powerline_yolov5s\src\atlas\api_server.py -Force
Copy-Item .\src\atlas\acl_runtime.py .\atlas_deploy_powerline_yolov5s\src\atlas\acl_runtime.py -Force
Copy-Item .\src\atlas\detector_om.py .\atlas_deploy_powerline_yolov5s\src\atlas\detector_om.py -Force
Copy-Item .\src\atlas\yolo_postprocess.py .\atlas_deploy_powerline_yolov5s\src\atlas\yolo_postprocess.py -Force
```

如果重新导出了 ONNX，也同步到部署目录：

```powershell
Copy-Item .\models\onnx\best.onnx .\atlas_deploy_powerline_yolov5s\models\onnx\best.onnx -Force
```

重新打包：

```powershell
Compress-Archive `
  -Path .\atlas_deploy_powerline_yolov5s\* `
  -DestinationPath .\atlas_deploy_powerline_yolov5s.zip `
  -Force
```

## 8. 上传部署包到 Atlas

在 PC PowerShell 中执行：

```powershell
scp .\atlas_deploy_powerline_yolov5s.zip HwHiAiUser@192.168.137.100:/home/HwHiAiUser/
```

登录 Atlas：

```powershell
ssh HwHiAiUser@192.168.137.100
```

在 Atlas 上解压：

```bash
cd /home/HwHiAiUser
rm -rf atlas_deploy_powerline_yolov5s
unzip -o atlas_deploy_powerline_yolov5s.zip -d atlas_deploy_powerline_yolov5s
cd atlas_deploy_powerline_yolov5s
```

## 9. Atlas 侧环境准备

加载 Ascend 环境：

```bash
source /usr/local/Ascend/ascend-toolkit/set_env.sh
```

安装 Python 依赖：

```bash
python3 -m pip install -r requirements/atlas.txt
```

检查关键文件：

```bash
ls configs/classes.txt
ls models/onnx/best.onnx
ls src/atlas/api_server.py
```

## 10. ONNX 转 OM

如果已经有：

```text
models/om/powerline_yolov5s.om
```

可以跳过本节。

如果没有 OM 模型，在 Atlas 上执行 ATC 转换。

Atlas 200I DK A2 常用 `Ascend310B4`：

```bash
source /usr/local/Ascend/ascend-toolkit/set_env.sh

bash scripts/atc_convert.sh \
  models/onnx/best.onnx \
  Ascend310B4 \
  models/om \
  powerline_yolov5s \
  images \
  1,3,640,640
```

转换成功后确认：

```bash
ls -lh models/om/powerline_yolov5s.om
```

## 11. 启动 Atlas 后端

先清理旧进程：

```bash
fuser -k 8000/tcp || true
```

启动后端：

```bash
fuser -k 8000/tcp
cd /home/HwHiAiUser/atlas_deploy_powerline_yolov5s
source /usr/local/Ascend/ascend-toolkit/set_env.sh
export PYTHONPATH=$PWD:$PYTHONPATH

python3 -u -m src.atlas.api_server \
  --model-registry configs/model_registry.json \
  --camera-source 0 \
  --host 0.0.0.0 \
  --port 8000 \
  --imgsz 640 \
  --conf-thres 0.25 \
  --iou-thres 0.45 \
  --device-id 0 \
  --video-sample-fps 8 \
  --video-max-frames 480
```

看到类似下面的信息说明 Flask 已启动：

```text
Running on http://0.0.0.0:8000
```

不要关闭这个终端。关闭后后端也会停止。

## 12. 验证 Atlas 后端

在 Atlas 本机测试：

```bash
curl http://127.0.0.1:8000/api/health
```

预期关键字段：

```json
{
  "status": "ok",
  "model_loaded": true
}
```

在 PC PowerShell 测试：

```powershell
Invoke-WebRequest -UseBasicParsing http://192.168.137.100:8000/api/health
```

如果 PC 访问不到，先检查：

- PC 和 Atlas 是否在同一网络。
- Atlas IP 是否正确。
- 后端是否使用 `--host 0.0.0.0`。
- 8000 端口是否被防火墙拦截。

## 13. 启动前端

在 PC PowerShell 中回到项目根目录：

```powershell
cd C:\Users\29951\OneDrive\Desktop\atlas_powerline_ai_experiment
```

启动静态前端：

```powershell
python -m http.server 5173 --bind 127.0.0.1 --directory frontend
```

如果 `python` 不可用，可以用 Python 绝对路径：

```powershell
& "C:\Users\29951\AppData\Local\Programs\Python\Python312\python.exe" `
  -m http.server 5173 `
  --bind 127.0.0.1 `
  --directory frontend
```

浏览器打开：

```text
http://127.0.0.1:5173/?api=http://192.168.137.100:8000
```

如果开发板 IP 不是 `192.168.137.100`，把 URL 中的 IP 换成实际 IP。

## 14. 前端功能使用

前端顶部有 Atlas 服务地址输入框。

确认它是：

```text
http://192.168.137.100:8000
```

点击连接或刷新状态后，正常情况下会显示后端在线和模型加载状态。

### 开发板摄像头

点击：

```text
开发板摄像头
```

前端会显示 `/api/stream` 返回的后端画框实时画面。

### 上传图片

点击：

```text
上传图片
```

选择图片后，前端会调用：

```text
POST /api/detect/image
```

后端返回已画框图片，前端直接显示后端 Atlas 结果。

### 上传视频

点击：

```text
上传视频
```

选择视频后，前端会调用：

```text
POST /api/detect/video/stream
```

后端会边解码、边推理、边返回画好框的 JPEG 帧。

RUNNING 阶段显示实时推理帧。RUNNING 结束后，可以拖动进度条查看对应时间点的检测帧。

## 15. 后端 API 说明

### GET /api/health

检查后端状态和模型加载状态。

```bash
curl http://192.168.137.100:8000/api/health
```

### GET /api/stream

开发板摄像头实时检测流，返回 MJPEG。

浏览器可直接打开：

```text
http://192.168.137.100:8000/api/stream
```

### GET /api/events

实时事件流，返回最近检测结果。

前端右侧指标和告警事件会使用这个接口。

### POST /api/detect/image

上传图片检测。

```bash
curl -F "file=@test.jpg" http://192.168.137.100:8000/api/detect/image
```

### POST /api/detect/video/stream

上传视频流式检测。

前端使用该接口读取 MJPEG 分片，不建议直接用普通浏览器打开。

## 16. 常见问题

### 16.1 model_loaded 是 false

优先看 `/api/health` 里的 `error`。

常见原因：

- `models/om/powerline_yolov5s.om` 不存在。
- `configs/classes.txt` 路径不对。
- 没有执行 `source /usr/local/Ascend/ascend-toolkit/set_env.sh`。
- CANN/ACL 环境没有正确安装。

### 16.2 前端连不上后端

检查 PC 是否能访问：

```powershell
Invoke-WebRequest -UseBasicParsing http://192.168.137.100:8000/api/health
```

如果失败，检查：

- IP 是否正确。
- Atlas 后端是否还在运行。
- 后端是否使用 `--host 0.0.0.0`。
- PC 和 Atlas 是否同网段。

### 16.3 摄像头打不开

在 Atlas 上确认摄像头编号：

```bash
ls /dev/video*
```

如果不是默认 `0`，启动后端时修改：

```bash
--camera-source 1
```

也可以先用 OpenCV 或系统工具测试摄像头是否可用。

### 16.4 上传视频看起来不够流畅

视频上传检测默认采样：

```text
--video-sample-fps 8
```

如果想更流畅，可以提高，例如：

```bash
--video-sample-fps 12
```

如果 Atlas 推理跟不上，降低这个值，例如：

```bash
--video-sample-fps 4
```

### 16.5 前端页面还是旧效果

浏览器强制刷新：

```text
Ctrl + F5
```

或者关闭浏览器后重新打开：

```text
http://127.0.0.1:5173/?api=http://192.168.137.100:8000
```

### 16.6 8000 端口被占用

在 Atlas 上执行：

```bash
fuser -k 8000/tcp || true
```

然后重新启动后端。

### 16.7 5173 端口被占用

PC 上换一个前端端口即可：

```powershell
python -m http.server 5174 --bind 127.0.0.1 --directory frontend
```

浏览器打开：

```text
http://127.0.0.1:5174/?api=http://192.168.137.100:8000
```

## 17. 交接检查清单

交接前建议逐项确认：

```text
[ ] PC 能打开前端 http://127.0.0.1:5173/
[ ] Atlas 后端能启动在 0.0.0.0:8000
[ ] PC 能访问 http://Atlas_IP:8000/api/health
[ ] /api/health 中 model_loaded 为 true
[ ] 前端能连接 Atlas 服务
[ ] 开发板摄像头实时流能显示
[ ] 上传图片能显示后端检测框
[ ] 上传视频 RUNNING 时能持续显示检测帧
[ ] 上传视频结束后进度条可拖动
[ ] atlas_deploy_powerline_yolov5s.zip 已重新打包
```

## 18. 推荐新人第一次运行顺序

新人第一次接手时，建议按这个最短闭环跑：

```text
1. PC 解压/打开项目。
2. PC 启动前端。
3. Atlas 解压 atlas_deploy_powerline_yolov5s.zip。
4. Atlas source Ascend 环境。
5. Atlas 确认 OM 模型存在。
6. Atlas 启动 api_server.py。
7. Atlas 本机 curl /api/health。
8. PC curl /api/health。
9. 浏览器打开前端并连接 Atlas。
10. 测试摄像头、图片、视频三种输入。
```

如果这 10 步都成功，说明项目运行链路已经完整打通。
