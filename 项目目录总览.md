# 项目目录总览

本文档用于说明 `atlas_powerline_ai_experiment` 项目中各个主要文件夹的作用，方便后续运行、答辩、交接和精简项目。

## 项目整体目标

本项目实现的是一个“输电线路智能巡检平台”，主要功能包括：

- 使用 YOLOv5 训练输电线路风险目标检测模型。
- 将模型从 `best.pt` 导出为 `ONNX`。
- 在 Atlas 200I DK A2 开发板上将 ONNX 转换为 `OM` 模型。
- 使用 pyACL 调用 Ascend NPU 完成推理。
- 通过 Flask 后端提供图片、视频、摄像头实时流检测接口。
- 通过浏览器前端展示检测画面、风险等级和告警事件。

## 一级目录说明

| 文件夹 | 作用 | 
|---|---|---|
| `.idea` | PyCharm/JetBrains 项目配置目录 
| `.venv` | PC 端 Python 虚拟环境 
| `atlas_deploy_powerline_yolov5s` | 上传到 Atlas 开发板的部署目录 |
| `configs` | 类别文件、数据集配置文件 
| `datasets` | 原始数据、训练数据和测试素材 
| `docs` | 项目文档、实验报告、说明材料 
| `frontend` | PC 浏览器前端页面 
| `models` | 训练权重、ONNX 模型、OM 模型位置 
| `scripts` | 训练、导出、转换、环境检查等脚本 
| `src` | 项目核心源码，包括 PC 工具和 Atlas 后端 
| `yolov5` | YOLOv5 官方仓库代码 | 

## 推荐阅读顺序

如果是第一次接手项目，建议按下面顺序看：

1. `docs/folder_guides/00_项目目录总览.md`
2. `docs/folder_guides/05_datasets.md`
3. `docs/folder_guides/04_configs.md`
4. `docs/folder_guides/08_src.md`
5. `docs/folder_guides/07_frontend.md`
6. `docs/folder_guides/03_atlas_deploy_powerline_yolov5s.md`
7. `docs/12_从零运行项目.md`
8. `docs/电力行业应用实践报告.md`

## 最短运行链路

如果只是演示系统，不重新训练，最短链路是：

```text
Atlas 后端：
atlas_deploy_powerline_yolov5s/
  models/om/powerline_yolov5s.om
  configs/classes.txt
  src/atlas/api_server.py

PC 前端：
frontend/
```

启动前端：

```powershell
python -m http.server 5173 --bind 127.0.0.1 --directory frontend
```

浏览器访问：

```text
http://127.0.0.1:5173/
```

